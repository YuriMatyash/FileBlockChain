# PrintChain root environment placeholders
# Copy to .env for local Hardhat development. Do not commit real secrets.

# Local Hardhat network defaults only. Testnet/mainnet settings are intentionally omitted in Phase 0.
HARDHAT_CHAIN_ID=31337
# Optional Phase 6 upload proxy endpoint. Leave empty for mock/demo mode.
VITE_IPFS_UPLOAD_ENDPOINT=
